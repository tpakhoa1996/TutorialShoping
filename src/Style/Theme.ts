import { PixelRatio } from "react-native";

export const theme = {
    textSize: {
        big: 20 * PixelRatio.getFontScale(),
        medium: 20 * PixelRatio.getFontScale(),
        normal: 20 * PixelRatio.getFontScale(),
    }
};